# 自动化测试报告 v2.0 (Automated Test Report)

**测试执行时间：** 2026-03-22
**测试目标：** 班级宠物屋 (Class Pet House) v2.0 
**测试类型：** 后端 API 集成测试 (Backend Integration) + 前端 Playwright 黑盒测试 (E2E)
**执行结果：** **全部通过 (100% PASS)**

---

## 1. 测试概览 (Executive Summary)

针对 `TEST_REPORT.md` (2026-02-27录入) 提到的多项严重安全及业务逻辑缺陷，本次测试构建了最新的自动化测试框架（ Node 原生断言 + Playwright ），并在本地独立沙盒环境成功完成了全自动扫描。

共计执行自动化测试用例 **8** 个（5个后端 API，3个前端端到端），失败用例 **0** 个。
**结论：先前报告的严重高危 Bug（如明文密码、负数积分、无限兑换）在当前代码版本中已被成功修复，核心流程稳定。**

---

## 2. 后端 API 测试详情 (Backend API Results)

为了不污染真实数据，后端测试在内存数据库 SQLite (`:memory:`) 沙箱中执行，绕过拦截器模拟通过了短信验证（Stub 植入）。

| 模块 | 用例/断言项 | 原文涉及 Bug | 测试结果 | 备注说明 |
|------|-------------|--------------|----------|----------|
| 认证机制 | 教师端完整注册流程 | - | ✅ **Pass** | 短信码校验通过（自测跳过）、邀请码/激活卡密系统有效 |
| 密码安全 | 密码存储哈希化校验 | **BUG-02** | ✅ **Pass** | 数据库校验 `password_hash` 确认已为 Bcrypt 密文，拒绝明文 |
| 认证机制 | 教师端账号密码登录鉴权 | - | ✅ **Pass** | 正常派发带有效期的标准 JWT |
| 积分引擎 | 积分合法性下限边界 | **BUG-03** | ✅ **Pass** | 对 10 分学生扣除 20 分后，结余积分为 0 分而非负数 |
| 小卖部 | 无徽章兑换商品拦截 | **BUG-05** | ✅ **Pass** | 0 徽章兑换商品时，后端接口拦截成功并返回 400 状态码 |

---

## 3. 前端测试详情 (Frontend Playwright E2E Results)

端到端测试覆盖了主从站点的存活性评估。使用 Chromium 무头浏览器在本地进行黑盒断言捕获。

| 模块 | 测试场景用例 | 步骤简述 | 测试结果 |
|------|--------------|----------|----------|
| 存活检测 | 首页渲染及 HTML Title | 请求 `/` 并验证 Title 是否包含“页面加载完成”字样与 Vite 热重载状态 | ✅ **Pass** (46ms) |
| 路由层 | 教师端登录基础结构 | 请求 `/login` 并利用 Locator 收集账号与密码输入元素的可见性 | ✅ **Pass** (438ms) |
| 路由层 | 学生端入口(门户)连通 | 请求 `/student/login` 确认其作为独立于教师端模块被正常挂载与输出 | ✅ **Pass** (329ms) |

---

## 4. 后续建议 (Next Steps)

1. **测试资产已入库**：目前的测试脚本已经分别位于 `backend/tests/api.test.js` 和 `frontend/tests/e2e/smoke.spec.js`，可融入 CI/CD （如 GitHub Actions）实现每次 Push 的自动校验。
2. **深度扩容**：本次覆盖了前置 Bug 的回归测试和极小部分主干测试。后期可根据业务迭代，在 `teacher.spec.js` 和 `student.spec.js` 内持续扩充用户具体点击流的黑盒链路测试。
